const initialState = {
    auth: false
};
export default function profile(state = initialState, action) {
    switch (action.type) {
        default:
        {
            return state;
        }
    }
}